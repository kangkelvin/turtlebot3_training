local_costmap:
  global_frame: odom
  robot_base_frame: base_link
  update_frequency: 1.0
  publish_frequency: 1.0
  static_map: false
  rolling_window: true
  width: 10.0
  height: 10.0
  origin_x: 0.0 # only works for static map
  origin_y: 0.0 # only works for static map
  resolution: 0.05
